---

kind: Application
apiVersion: argoproj.io/v1alpha1

metadata:
  name: app-netbox
  namespace: argocd

spec:
  project: tech

  destination:
    name: funny-bell
    namespace: netbox

  source:
    repoURL: https://github.com/agrrh/infrastructure
    targetRevision: HEAD
    path: kubernetes/tech/applications/netbox
    directory:
      recurse: true

  syncPolicy:
    automated:
      selfHeal: true
      prune: true
