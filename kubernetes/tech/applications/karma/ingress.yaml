---

apiVersion: traefik.io/v1alpha1
kind: IngressRoute

metadata:
  name: karma-http
  namespace: metrics

spec:
  entryPoints:
    - web
  routes:
    - kind: Rule
      match: Host(`karma.agrrh.com`)
      middlewares:
        - name: whitelist
          namespace: kube-system
      services:
        - kind: Service
          name: karma
          namespace: metrics
          port: 80
