---

apiVersion: apps/v1
kind: Deployment

metadata:
  name: bot
  namespace: tg-dukhota-bot
  labels:
    app: dukhota

spec:
  replicas: 1
  strategy:
    type: Recreate

  selector:
    matchLabels:
      app: dukhota
  template:
    metadata:
      labels:
        app: dukhota
    spec:
      containers:
        - name: main
          image: agrrh/tg-dukhota-bot:0.1.1
          env:
            - name: APP_REDIS_HOST
              value: redis

            - name: APP_TG_TOKEN
              valueFrom:
                secretKeyRef:
                  name: config
                  key: tg_token
