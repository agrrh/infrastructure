---
kind: Application
apiVersion: argoproj.io/v1alpha1

metadata:
  name: app-vector
  namespace: argocd

spec:
  project: tech

  destination:
    name: brave-booth
    namespace: default

  source:
    repoURL: https://github.com/agrrh/infrastructure
    targetRevision: HEAD
    path: kubernetes-v2/tech/applications/vector
    directory:
      recurse: true

  syncPolicy:
    automated:
      selfHeal: true
      prune: true
